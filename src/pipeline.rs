//! Coder pipeline dispatch: given a `Folder` and packed bytes, produce unpacked bytes.
//!
//! This module will be expanded into a `pipeline/` subdirectory by task #6.

/// Placeholder — real dispatch (Copy coder + trait object) lands in task #6.
pub fn _placeholder() {}
