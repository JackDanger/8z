//! High-level archive reading: open a 7z file, iterate entries, extract.
//!
//! Real implementation lands in task #6.

/// An opened 7z archive (placeholder).
pub struct Archive;

/// Iterator over the entries in an [`Archive`] (placeholder).
pub struct ArchiveReader;
